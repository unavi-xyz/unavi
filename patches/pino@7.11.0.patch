diff --git a/lib/tools.js b/lib/tools.js
index 3dd5e1880688b8a0f251119928d2fa6a40b03ca0..9635cfb79d61575bc43c215c918a827b2fe690b3 100644
--- a/lib/tools.js
+++ b/lib/tools.js
@@ -215,16 +215,7 @@ function getPrettyStream (opts, prettifier, dest, instance) {
     prettifier = prettifier.bind(instance)
     return prettifierMetaWrapper(prettifier(opts), dest, opts)
   }
-  try {
-    const prettyFactory = require('pino-pretty').prettyFactory
-    prettyFactory.asMetaWrapper = prettifierMetaWrapper
-    return prettifierMetaWrapper(prettyFactory(opts), dest, opts)
-  } catch (e) {
-    if (e.message.startsWith("Cannot find module 'pino-pretty'")) {
-      throw Error('Missing `pino-pretty` module: `pino-pretty` must be installed separately')
-    };
-    throw e
-  }
+
 }
 
 function prettifierMetaWrapper (pretty, dest, opts) {