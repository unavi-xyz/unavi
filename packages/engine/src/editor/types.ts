export enum Tool {
  Translate = "translate",
  Rotate = "rotate",
  Scale = "scale",
}

export enum ColliderType {
  Box = "box",
  Sphere = "sphere",
  Capsule = "capsule",
  Cylinder = "cylinder",
  Mesh = "mesh",
  Hull = "hull",
}

export enum RigidBodyType {
  Static = "static",
  Dynamic = "dynamic",
}
