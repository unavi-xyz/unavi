{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**", "dist/**", ".next/**"]
    },
    "client#build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"],
      "env": [
        "ALCHEMY_ID",
        "BUNDLE_ANALYZE",
        "NEXT_AUTH_SECRET",
        "NEXT_PUBLIC_AXIOM_INGEST_ENDPOINT",
        "NEXT_PUBLIC_IPFS_AUTH",
        "NEXT_PUBLIC_IPFS_ENDPOINT",
        "NEXT_PUBLIC_IPFS_GATEWAY",
        "NODE_ENV",
        "S3_ACCESS_KEY_ID",
        "S3_BUCKET",
        "S3_ENDPOINT",
        "S3_REGION",
        "S3_SECRET",
        "VERCEL_URL"
      ]
    },
    "host#build": {
      "dependsOn": ["^build"],
      "env": ["RTC_MAX_PORT", "RTC_MIN_PORT"]
    },
    "lint": {
      "outputs": []
    },
    "deploy": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
