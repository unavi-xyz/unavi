[project]
name     = "unavi-server"
revision = "1.0"

[profiles.default]
UNAVI_DOMAIN = { default = "node.unavi.xyz", description = "Domain the server's did:web identity resolves from" }

[profiles.development]
UNAVI_DOMAIN = { default = "localhost:5000" }
