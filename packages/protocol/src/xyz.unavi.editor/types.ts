export enum ColliderType {
  Box = "box",
  Sphere = "sphere",
  Capsule = "capsule",
  Cylinder = "cylinder",
  Mesh = "mesh",
  Hull = "hull",
}

export enum RigidBodyType {
  Static = "static",
  Dynamic = "dynamic",
}
