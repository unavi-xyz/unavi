package wired:script;

world guest {
  export guest-api;
}

interface guest-api {
  // Called once on instantiation.
  init: func();

  // Called every frame, before rendering.
  render: func();

  // Called every tick, on a fixed interval.
  tick: func();
}
