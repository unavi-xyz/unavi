query GetPublications($profileId: ProfileId!) {
  publications(
    request: {
      profileId: $profileId
      publicationTypes: [POST, COMMENT, MIRROR]
      limit: 10
    }
  ) {
    items {
      __typename
      ... on Post {
        ...PostFields
      }
      ... on Comment {
        ...CommentFields
      }
      ... on Mirror {
        ...MirrorFields
      }
    }
    pageInfo {
      prev
      next
      totalCount
    }
  }
}
