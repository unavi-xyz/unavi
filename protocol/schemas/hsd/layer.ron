(
    id: "wired/hsd/layer",
    version: 0,
    layout: Struct({
        "id": String,
        "opinions": Map(Struct({
            "delete": Optional(List(String)),
            "update": Optional(Map(Any)),
        })),
    }),
)