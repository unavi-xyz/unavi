query GetProfilesByAddress($address: EthereumAddress!) {
  profiles(request: { ownedBy: [$address], limit: 20 }) {
    items {
      id
      name
      handle
      isDefault
    }
    pageInfo {
      prev
      next
      totalCount
    }
  }
}
