(
    id: "wired/hsd",
    version: 0,
    layout: Struct({
        "layers": MovableList(Struct({
            "id": String,
            "opinions": Map(Struct({
                "delete": Optional(List(String)),
                "set": Optional(Map(Any)),
            })),
        })),
        "nodes": Tree(Struct({
            "attributes": Map(Any),
            "id": String,
        })),
    }),
)