{
  "name": "@wired-xr/client-db",
  "version": "0.1.0",
  "private": true,
  "license": "GPL-3.0-or-later",
  "scripts": {
    "build": "docker compose build",
    "codegen": "prisma generate",
    "dev": "yarn start",
    "docker:down": "docker compose down",
    "docker:prune": "yarn docker:down && docker image rm -f client-db-api",
    "docker:up": "docker compose up",
    "migrate": "prisma migrate dev",
    "start": "yarn docker:prune && yarn build && yarn docker:up"
  },
  "devDependencies": {
    "prisma": "^4.1.1"
  }
}
