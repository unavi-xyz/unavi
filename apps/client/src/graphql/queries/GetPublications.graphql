query GetPublications($profileId: ProfileId!, $sources: [Sources!]) {
  publications(
    request: {
      profileId: $profileId
      publicationTypes: [POST]
      limit: 10
      sources: $sources
    }
  ) {
    items {
      __typename
      ... on Post {
        ...PostFields
      }
      ... on Comment {
        ...CommentFields
      }
      ... on Mirror {
        ...MirrorFields
      }
    }
    pageInfo {
      prev
      next
      totalCount
    }
  }
}
