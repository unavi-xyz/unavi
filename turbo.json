{
  "$schema": "https://turborepo.org/schema.json",
  "baseBranch": "origin/main",
  "pipeline": {
    "lint": {},
    "build": {
      "dependsOn": ["^build", "codegen"],
      "outputs": ["dist/**", ".next/**"]
    },
    "codegen": {
      "dependsOn": ["^build"]
    },
    "test": {
      "dependsOn": ["^build", "codegen"]
    },
    "dev": {
      "dependsOn": ["^build"],
      "cache": false
    }
  }
}
