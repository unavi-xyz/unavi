(
    id: "wired/space",
    version: 0,
    layout: Struct({
        "name": String,
        "objects": List(Struct({
            "record": RecordId,
            "rotation": Optional(List(F64)),
            "scale": Optional(List(F64)),
            "translation": Optional(List(F64)),
        })),
    }),
)