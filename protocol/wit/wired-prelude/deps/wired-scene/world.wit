package wired:scene;

world prelude {
  import types;
}

interface types {
  use wired:math/types.{vec3};

  record node-id {
    /// The document the node belongs to.
    doc: list<u8>,
    /// The local in-document node ID.
    id: s64,
  }

  resource node {
    id: func() -> node-id;
  }
}
